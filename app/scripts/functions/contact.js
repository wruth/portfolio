(function (Portfolio) {

    var x = '@',
        y = 'ward';

    /**
     * Attempt to obfuscate address through js handling.
     *
     * @function  contact
     */
    Portfolio.functions.contact = function contact () {
        var a = 'ruth',
            b = 'gmail',
            c = 'com';

        if (ga) {
            ga('send', 'event', 'button', 'click', 'contact');
        }

        window.location = 'ma' + 'ilto:' + y + '.' + a + x + b + '.' + c;
    };

}(window.Portfolio));
